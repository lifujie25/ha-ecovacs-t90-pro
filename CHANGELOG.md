# 更新日志

## 1.0.4

- 修复国行 T90 Pro 对区域清扫返回 `20011 / unknown type` 的问题。
- 将 Home Assistant 的房间清扫请求转换为设备实际支持的
  `clean_V2 / freeClean` 参数，并保留清扫次数。
- 短暂实机回归测试覆盖区域清扫与停止命令。

## 1.0.2

- 修复国行 T90 Pro 区域清扫仍使用旧版 `clean` 指令、机器人返回
  `20003 / rcp not support` 的问题。
- 自动清扫与区域清扫改用设备实际支持的 `clean_V2` 协议。
- 地图卡片增加命令发送中、成功和失败提示。

## 1.0.1

- 将自带地图卡片自动登记为持久化 Lovelace 资源。
- 修复重启后新标签页可能错过前端模块通知、地图卡片不显示的问题。
- YAML 资源模式继续使用当前会话自动加载作为兼容方案。

## 1.0.0

- 首个 HACS 正式版本。
- 修复中国区 T90 Pro `guaexd` 硬件类和 V2 地图协议。
- 支持地图、房间、位置、充电座和清扫轨迹。
- 内置可缩放、可选房间、可发起分区清扫的中文地图卡片。
- 地图卡片随集成自动加载，提供图形化实体选择。
